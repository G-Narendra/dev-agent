Build a complete, production-level portfolio website for Narendra Modi in the "portfolio_narendra" folder. This must be a FULL-STACK application with real functionality, NOT a static skeleton.

REQUIREMENTS:

1. FIRST — Research Phase:
   - Use web_search to research Narendra Modi's official website (narendramodi.in) for accurate biographical data, achievements timeline, photo URLs, and policy information
   - Use read_url to extract real content from Wikipedia and official government sources
   - Use the free_api tool to find relevant data (REST Countries for India info, etc.)
   - Use web_search to find high-quality, publicly available images of Narendra Modi (use hyperlinks to images, NOT downloaded files)
   - Research modern portfolio website design trends and choose the best layout

2. TECHNOLOGY STACK:
   - Backend: Node.js + Express.js
   - Templating: EJS with a shared layout template
   - Styling: Custom CSS (modern, responsive, professional)
   - Database: JSON file storage (no external DB needed)
   - Use npm packages installed locally (run: cd portfolio_narendra && npm init -y && npm install express ejs body-parser)

3. PAGES TO CREATE (minimum 8 pages with REAL content):

   a) index.html - Homepage with hero banner, brief intro, key achievements highlights, latest news section
   b) about.html - Full biography, early life, political career timeline (interactive timeline), education details
   c) achievements.html - Major achievements with categories (Economic reforms, Digital India, Make in India, Swachh Bharat, Foreign Policy, etc.) — use icons and cards
   d) gallery.html - Photo gallery with categorized images (use public image URLs, NOT local files), lightbox viewing
   e) timeline.html - Interactive scrollable timeline of key events from 1950 to present
   f) contact.html - Contact form with form validation (client-side + server-side), message storage
   g) projects.html - Flagging government schemes and projects (Jan Dhan, Ujjwala, Digital India, Smart Cities, etc.) with descriptions, status, and links
   h) news.html - Latest news section (simulated with JSON data)

4. DESIGN STANDARDS:
   - Professional, modern design similar to narendramodi.in
   - Color scheme: Saffron, White, Green (Indian flag colors) with modern gradients
   - Fully responsive (mobile, tablet, desktop)
   - Smooth scroll navigation
   - CSS animations on scroll (fade-in, slide-up effects)
   - Dark/Light mode toggle
   - Loading spinner for page transitions
   - Professional typography (use Google Fonts: Poppins + Playfair Display)
   - Footer with social media links and copyright

5. BACKEND FUNCTIONALITY:
   - Express server with proper routing (Router-based)
   - Contact form handler that saves messages to a JSON file
   - API endpoints: GET /api/projects, GET /api/timeline, GET /api/news
   - 404 error page
   - Static file serving for CSS/images
   - Environment configuration (PORT from env or default 3000)

6. QUALITY:
   - Every page must have REAL content about Narendra Modi (not placeholder text)
   - Use semantic HTML5 (header, nav, main, section, article, footer)
   - Include meta tags for SEO on every page
   - Include favicon reference
   - Proper error handling on both client and server
   - Code should be clean, well-commented

7. AFTER CREATING ALL FILES:
   - Run: cd portfolio_narendra && npm install
   - Verify the server starts: cd portfolio_narendra && timeout 5 node server.js
   - Report which files were created and their sizes

DO NOT stop until EVERY page is complete with REAL content. Do NOT create skeleton files. Every page must have substantial content (minimum 100 lines per page).